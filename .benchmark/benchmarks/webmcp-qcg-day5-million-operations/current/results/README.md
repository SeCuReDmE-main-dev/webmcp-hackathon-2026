# Results landing area

This directory contains Action 140 local receipts only: `windows-local.json` is a passing Windows Node engine baseline; `multipass-ubuntu-baseline-blocked.json` records a blocked Ubuntu attempt with zero operations. A future authorized run may place one `qcg-benchmark-result.v1` JSON receipt per sandbox/gate here, alongside the generated preparation plan and aggregate summary. Do not place secrets, raw Q#, URLs, credentials or provider diagnostics in this directory.
