# Video decision — 2026-08-29

Status: **superseded by the Day 7 two-take capture; production pending**

## Canonical format

The earlier live-introduction plus NotebookLM structure is replaced by a simpler
two-take production. NotebookLM may help with private preparation, but it is no
longer required in the final cut.

- **Take 1 — direct screen proof:** Jean-Sebastien submits the production prompt
  before recording. Codex preflights the clean desktop, live QCG page, Chrome
  side panel and recording controls, then remains active. At the retained start,
  Codex activates Screencastify through approved window control and immediately
  drives the real QCG interaction. Prompt submission and model latency never
  appear in the video.
- **Take 2 — author voice-over:** Jean-Sebastien records the complete narration
  after the screen proof exists, so every sentence can be synchronized with an
  observed result.
- Final duration: target 2:50–2:55; hard limit below 3:00.
- Product and first native agent action: visible within the first 10 seconds;
  absolute hard limit 12 seconds.
- Audio: supplied by the second take.

The retained screen proof joins two evidence lifecycles rather than presenting
a generic feature tour. First: inspect the artifact, evaluate the call, stop for
the human decision, simulate locally and export the receipt. Second: record a
real Codex observation, pass a sanitized packet to native Gemini in DevTools,
import Gemini's real response as untrusted evidence, and let the human
acknowledge the collaboration. Companion open, same-tab attachment and the
Gemini relay must all pass release QA before recording.

Between those lifecycles, a 13-second `Access` segment demonstrates that the
same legible structure serves direct human use and agent-assisted use. The
panel changes presentation only; it never changes a decision, receipt or effect
counter and is presented as a useful direct-use mechanism rather than a
conformance certificate.

The voice-over may describe genuine commands in present tense, but it must
never imply that a staged voice request triggered the already recorded actions.
The final edit should carry the discreet caption `Live screen proof • narration
recorded afterward` during the opening sequence.

The screen take remains the technical source of truth. The voice-over explains
what the retained take visibly proves and introduces no additional claim.

## Required proof on screen

1. A real QCG page loads from the stable public URL.
2. Native WebMCP discovers the tools.
3. One artifact is inspected and evaluated.
4. The decision, reason codes and next action are visible.
5. Human authority is visible as a state separate from readiness.
6. A bounded local simulation occurs only on the `simulate_first` path after consent.
7. The evidence receipt shows the artifact/profile references and `external_provider_calls: 0`.
8. The WebMCP or Activity view confirms the progressive tool lifecycle and the
   corresponding invocation trail.
9. The Access panel visibly changes a browser-local preference while preserving
   the active authority and effect state. The narration connects readable
   controls for people with clear names, roles and states for agents.
10. Companion synchronization and one genuine Gemini manual relay are verified
   against the same tab and session before recording.
11. Codex and Gemini may observe, challenge and request review. Only the human
     acknowledges the review, and that action never changes quantum consent.

## Editorial responsibilities

Jean-Sebastien submits the production mission before the camera rolls, grants
the already reviewed screen-control scope, performs the one visible
human-authority action and records the second take. Codex prepares the surfaces,
starts Screencastify at the evidence boundary and performs the bounded browser
and WebMCP work shown in the first take. The application interaction remains the
source of technical proof. Every spoken factual claim maps to
`evidence/SOURCE_CLAIM_REGISTRY_2026-08-29.md`.

## Final QA

- Measure the exported video duration rather than relying on timeline estimates.
- Record Take 1 without microphone narration; preserve cursor movement and the
  actual interface responses.
- Record Take 2 only after Take 1 passes the technical proof checklist.
- Listen to the entire audio track with headphones and speakers.
- Confirm text remains legible at 1080p and on a laptop-sized player.
- Confirm no prompt submission, model startup or idle wait appears in the final cut.
- Confirm QCG is already ready when recording begins and the first native tool
  action appears within 10 seconds; reject any take that exceeds 12 seconds.
- Confirm Codex never clicks or fabricates the human decision.
- Confirm the author click is visible before the simulation tool appears.
- Confirm the retained sequence reaches the receipt, WebMCP lifecycle and
  activity evidence without becoming a button-by-button tour.
- Confirm the Access sequence takes approximately 13 seconds, demonstrates a
  real preference change and never implies accessibility certification or an
  assistive-technology replacement.
- Confirm any Companion footage was captured only after open, attachment and
  same-session synchronization passed QA. If the genuine relay fails, postpone
  recording; a closing slide cannot replace the retained A2A proof.
- Confirm the voice-over never poses as the live trigger for prerecorded
  actions.
- Confirm the stable URL and public repository appear accurately.
- Confirm the final cut contains no credentials, private paths or temporary preview URL.

Final upload and Devpost submission remain author-controlled actions.
