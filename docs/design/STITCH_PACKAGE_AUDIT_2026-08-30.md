# Stitch package audit — Spring workbench

- Date: 2026-08-30
- Source: local ignored archive `asset/.stitch/stitch_webmcp_quantum_call_gate_workbench.zip`
- Archive SHA-256: `f664ad9e6f8ff5771a0ebc33fb099fe45a98643313fa54400b37853fee1330ef`
- Compressed size: 65,602,315 bytes
- ZIP entries: 233
- Regular files: 148
- Uncompressed size: 67,025,882 bytes
- Path traversal entries: 0
- Runtime status: reference package only

## Inventory

The package contains 80 PNG files, 54 static HTML files, 10 Markdown files, 3 JSON files and 1 text file. It includes desktop, tablet and mobile explorations for Spring, Autumn, Winter and Summer, plus empty, loading, error and success states.

The HTML files are static Stitch renderings. They primarily load Tailwind through a CDN and do not constitute the React/Vite runtime used by QCG. A few variants introduce Three.js or shader experiments; those remain outside the frozen product.

## Privacy gate

No credential, environment file, token, password or private key was detected. Two internal metadata documents contain author-local absolute paths. The archive therefore remains ignored and must not be committed, published or redistributed in its current form.

## Adopt

- The central gate-chamber composition.
- Separate provenance and evidence rails.
- The Spring high-signal button hierarchy.
- Responsive references for desktop and mobile.
- Explicit empty, loading, error and success-state coverage.
- Botanical circuit motifs used as restrained structure rather than decorative wallpaper.
- Four-season visual continuity with one unchanged semantic workflow.

## Reference and redraw

- Spring desktop and mobile workbench references.
- Spring article layouts and button maps.
- Side-panel proportions and navigation density.
- Autumn, Winter and Summer variants for later seasonal consistency.
- The quantum-botany synthesis image as a mood reference only.

Every adopted visual must be redrawn with QCG's real components, real labels, real states and existing accessibility constraints. Raw screenshots do not become production UI assets.

## Reject

- Generated HTML as application code.
- CDN runtime dependencies.
- Fake metrics, signatures, hashes, clearances or QPU states.
- Any `Execute`, `Execute Final` or equivalent command that conflicts with the preflight authority boundary.
- Marketing screenshots, app-store frames, covers and promotional infographics inside the product interface.
- Animated or shader variants until reduced-motion, performance and accessibility gates are proven.

## Integration boundary

The stable public site remains unchanged. The frozen QCG decision engine, WebMCP surfaces, F12 panel and human-authority contracts remain the source of truth. A future graphics pass may change tokens, layout, typography, icons and local SVG/CSS motifs only after author review; it must preserve the current DOM semantics, test suite and authority boundaries.
