# Actions 183–199 — G1 engine surgery receipt

Status: `G1_PASS`

Captured: 2026-09-02 01:45 EDT (America/Toronto)

## Ground

- Baseline checkpoint: `docs/evidence/ACTIONS_175_182_G0_BASELINE_2026-09-02.md`.
- Scope remained the standalone QCG product with four progressive quantum tools, four collaboration tools, local Q# and OpenQASM execution only, and human authority.
- The G0 path inventory is intentionally point-in-time. Product and test files added by Actions 183–199 are the classified G1 delta, not an unexplained change to Action 176.
- Gemini findings were adjudicated by `docs/evidence/GEMINI_FINDING_DISPOSITION_2026-09-02.md`; rejected findings did not alter the digest, receipt v3 or consent policy.

## Execute

- Added a concurrent-simulation test proving one consent is consumed synchronously before the Worker awaits.
- Replaced full WebMCP teardown/re-registration with independent per-tool controllers, pending state and failure state.
- Added lifecycle tests for stable base tools, conditional tools, delayed registration and sibling registration failure.
- Added polite success announcements and assertive bounded error announcements.
- Narrowed sanitizer patterns so ordinary `body`, `stack` and `trace` prose passes while credentials, raw code, paths, stack traces and HTTP bodies remain blocked across page, snapshot and broker layers.
- Completed Markdown human-decision evidence with choice, ID, justification and timestamp.
- Hardened IndexedDB blocked/upgrade handling, safe result capture, connection close and recovery tests.
- Replaced raw Worker errors with finite public error codes and bounded client messages.
- Removed artifact-prefix inference in favor of exact demonstration selection and manifest provenance.
- Aligned Access semantics, Light sage tokens, orange attention cues and Companion-width reflow without changing the accepted Web Dark palette.

## Validate

Clean candidate:

`C:\Users\jeans\AppData\Local\Temp\webmcp-qcg-root-validation-20260902014217`

- `npm ci`: 108 packages installed; 0 vulnerabilities.
- `npm test`: 13 test files, 87 tests passed.
- `npm run build`: passed; 131 modules transformed.
- Application JS: 387.92 kB uncompressed, 114.17 kB gzip.
- CSS: 18.35 kB uncompressed, 4.59 kB gzip.
- Worker JS: 34.37 kB.
- QDK WASM: 6,066.57 kB uncompressed, 2,158.12 kB gzip; excluded from the application-JS budget by design.
- Companion source gates passed: MV3/permission validation, trusted open/close handshake, snapshot lifecycle and low-glare Light contrast.
- `git diff --check`: passed; only line-ending conversion notices were emitted.

The source `node_modules` directory was not rewritten because the open development server holds the Rolldown native binary. Clean-copy validation avoids terminating the author's browser or development process and proves installation from the lockfile.

## Output

- Engine, WebMCP lifecycle, Worker, storage, export, sanitizer and accessibility changes are present in the working candidate.
- Evidence schema remains `webmcp-qcg.evidence-receipt.v3`.
- No QPU, provider, payment or external execution path was added.
- No deployment, tag, commit or public package was changed by this checkpoint.

## Receipt

Actions 183–199 are `DONE`. Checkpoint G1 is `PASS`.

Residual package gate: the public production and development Companion ZIPs still contain the preceding v0.2.4 runtime and therefore intentionally fail package parity. They must be regenerated from the final validated extension source at Actions 220–221, after real-browser G2.
