{
  "name": "webmcp-qcg",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run --pool=threads --maxWorkers=1 --no-file-parallelism",
    "test:contracts": "vitest run src/services.test.ts --pool=threads --maxWorkers=1 --no-file-parallelism",
    "eval:smoke": "npx -y webmcp-evals@0.0.4 --chrome-channel chrome --reporter console json --output-dir ../../evidence/qa/webmcp-evals smoke -u http://127.0.0.1:5173/?eval_fixture=simulate-first -e evals/qcg-smoke.json -v",
    "eval:live": "npx -y webmcp-evals@0.0.4 --chrome-channel chrome --reporter console json --output-dir ../../evidence/qa/webmcp-evals-live smoke -u https://qcg.securedme.ca/?eval_fixture=simulate-first -e evals/qcg-smoke.json -v",
    "test:watch": "vitest --pool=threads --maxWorkers=1 --no-file-parallelism"
  },
  "dependencies": {
    "@nekuda/webmcp-sdk": "0.5.0",
    "@vitejs/plugin-react": "^6.1.1",
    "qsharp-lang": "1.31.0",
    "react": "latest",
    "react-dom": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@testing-library/react": "latest",
    "@testing-library/user-event": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "jsdom": "latest",
    "typescript": "latest",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
